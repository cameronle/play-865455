(()=>{'use strict';const board=document.getElementById('board'),scoreEl=document.getElementById('score'),bestEl=document.getElementById('best'),overlay=document.getElementById('overlay'),overlayTitle=document.getElementById('overlayTitle');let grid,score,best=Number(localStorage.getItem('play-2048-best')||0);const dirs={ArrowUp:{r:-1,c:0},w:{r:-1,c:0},W:{r:-1,c:0},ArrowDown:{r:1,c:0},s:{r:1,c:0},S:{r:1,c:0},ArrowLeft:{r:0,c:-1},a:{r:0,c:-1},A:{r:0,c:-1},ArrowRight:{r:0,c:1},d:{r:0,c:1},D:{r:0,c:1}};function emptyGrid(){return Array.from({length:4},()=>Array(4).fill(0))}function randomEmpty(){const cells=[];for(let r=0;r<4;r++)for(let c=0;c<4;c++)if(!grid[r][c])cells.push([r,c]);return cells.length?cells[Math.floor(Math.random()*cells.length)]:null}function addTile(){const p=randomEmpty();if(p)grid[p[0]][p[1]]=Math.random()<.9?2:4}function format(n){return String(n).padStart(6,'0')}function updateScore(){scoreEl.textContent=format(score);bestEl.textContent=format(best)}function draw(){board.innerHTML='';grid.forEach(row=>row.forEach(v=>{const cell=document.createElement('div');cell.className='cell'+(v?' tile-'+v:'');cell.textContent=v||'';board.appendChild(cell)}));updateScore()}function newGame(){grid=emptyGrid();score=0;overlay.classList.add('hidden');addTile();addTile();draw()}function slideLine(line){const values=line.filter(Boolean),result=[];for(let i=0;i<values.length;i++){if(values[i]===values[i+1]){const merged=values[i]*2;result.push(merged);score+=merged;i++}else result.push(values[i])}while(result.length<4)result.push(0);return result}function move(dir){const before=JSON.stringify(grid);if(dir.c!==0){for(let r=0;r<4;r++){let line=grid[r].slice();if(dir.c>0)line.reverse();line=slideLine(line);if(dir.c>0)line.reverse();grid[r]=line}}else{for(let c=0;c<4;c++){let line=[];for(let r=0;r<4;r++)line.push(grid[r][c]);if(dir.r>0)line.reverse();line=slideLine(line);if(dir.r>0)line.reverse();for(let r=0;r<4;r++)grid[r][c]=line[r]}}if(JSON.stringify(grid)!==before){if(score>best){best=score;localStorage.setItem('play-2048-best',String(best))}addTile();draw();if(!canMove()){overlayTitle.textContent='GAME OVER';overlay.classList.remove('hidden')}}}function canMove(){for(let r=0;r<4;r++)for(let c=0;c<4;c++){if(!grid[r][c])return true;if(c<3&&grid[r][c]===grid[r][c+1])return true;if(r<3&&grid[r][c]===grid[r+1][c])return true}return false}function keyMove(e){const dir=dirs[e.key];if(!dir)return;e.preventDefault();move(dir)}document.addEventListener('keydown',keyMove);document.getElementById('newGame').onclick=newGame;document.getElementById('tryAgain').onclick=newGame;document.querySelectorAll('[data-dir]').forEach(b=>b.addEventListener('pointerdown',()=>move(dirs[{up:'ArrowUp',down:'ArrowDown',left:'ArrowLeft',right:'ArrowRight'}[b.dataset.dir]])));let touchStart=null;board.addEventListener('touchstart',e=>{const t=e.changedTouches[0];touchStart={x:t.clientX,y:t.clientY}},{passive:true});board.addEventListener('touchend',e=>{if(!touchStart)return;const t=e.changedTouches[0],dx=t.clientX-touchStart.x,dy=t.clientY-touchStart.y;touchStart=null;if(Math.max(Math.abs(dx),Math.abs(dy))<25)return;move(Math.abs(dx)>Math.abs(dy)?(dx>0?dirs.ArrowRight:dirs.ArrowLeft):(dy>0?dirs.ArrowDown:dirs.ArrowUp))},{passive:true});newGame();bestEl.textContent=format(best)})();
