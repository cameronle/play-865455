{
  "name": "play-865455",
  "private": true,
  "scripts": {
    "generate:catalog": "node scripts/generate-catalog.js",
    "check:catalog": "node scripts/generate-catalog.js --check",
    "test": "npm run check:catalog && node --test tests/*.test.js"
  }
}
